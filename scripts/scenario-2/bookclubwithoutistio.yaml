apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookclub
  labels:
    app: bookclub
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bookclub
  template:
    metadata:
      labels:
        app: bookclub
    spec:
      containers:
        - name: bookclub
          image: fasttrackistio/bookclub:1.0.0
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: "1"
              memory: 100Mi
            requests:
              cpu: "0.5"
---
apiVersion: v1
kind: Service
metadata:
  name: bookclub
  labels:
    app: bookclub
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    app: bookclub
  type: NodePort